# ============================================================================
# CASE 4: GAUSSIAN BARRIER - SMOOTH POTENTIAL LANDSCAPE
# ============================================================================
# Physical System: Continuous potential profile (realistic approximation)
# Models: Field emission, molecular barriers, STM junctions, nuclear alpha decay
# Key Observable: WKB validity, reduced boundary reflections, adiabatic evolution
# Expected Transmission: 25-35% (higher than rectangular due to smooth edges)
# ============================================================================

scenario_name = Case 4 - Gaussian Barrier

# Spatial Grid
nx = 2048
x_min = -10.0         # [nm]
x_max = 10.0          # [nm]

# Temporal Integration
t_final = 5.0         # [fs]
n_snapshots = 200
adaptive_dt = true
dt_min = 0.001        # [fs]
dt_max = 0.01         # [fs]

# Gaussian Barrier Configuration
barrier_type = gaussian
barrier_height = 2.5      # [eV] - slightly higher peak
barrier_width = 1.5       # [nm] - σ parameter
barrier_center = 0.0      # [nm]

# Potential profile: V(x) = V₀·exp[-(x-x₀)²/(2σ²)]
# FWHM ≈ 2.35σ ≈ 3.5 nm (effective width)
# Gradient maximum: dV/dx|_max = V₀/(σe) ≈ 0.61 eV/nm at x = ±σ
# Compare rectangular: infinite gradient (discontinuous, unphysical)

# Particle Properties
particle_mass = 1.0       # [m_e]

# Initial State
x0 = -5.0
k0 = 6.0              # [nm⁻¹] - E_k ≈ 2.19 eV (mixed over/under barrier regime)
sigma = 0.4           # [nm]

# Environmental Effects (Laboratory Conditions, ~300K)
noise_amplitude = 0.10         # [eV] - realistic for RT experiments
noise_correlation_time = 0.25  # [fs] - optical phonon timescale
decoherence_rate = 0.0015      # [fs⁻¹] - T₂ ~ 667 fs

# Physical noise sources:
# - Thermal fluctuations: kT ~ 0.026 eV at 300K
# - Lattice phonons: ω ~ 4 THz
# - EM field fluctuations, defect scattering
# - Interface roughness (realistic devices)

# Output
save_netcdf = true
save_animation = true
fps = 30
dpi = 100

# WKB Approximation Validity:
# T ≈ exp[-2∫κ(x)dx], κ(x) = √[2m(V(x)-E)]/ℏ
# Accurate to ~1% for smooth potentials (slowly-varying condition satisfied)
# 
# Comparison with rectangular barrier (same V₀, similar width):
# Gaussian: T ~ 25-35% (smooth edges reduce reflection)
# Rectangular: T ~ 15-25% (sharp boundaries increase reflection)
# 
# Demonstrates universal tunneling across length scales:
# Nuclear (fm) → Atomic (Å) → Molecular (nm) → Mesoscopic (μm)
