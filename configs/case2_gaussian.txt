# Case 2: STM Tunneling (Gaussian Barrier)
# Smooth Gaussian potential barrier

scenario_name = Case 2 - Gaussian Barrier

# Spatial grid - CRITICAL FIX: Domain doubled
nx = 2048
x_min = -30.0         # CHANGED from -15.0 (doubled)
x_max = 30.0          # CHANGED from 15.0 (doubled)

# Time integration
t_final = 6.0         # fs
n_snapshots = 200
adaptive_dt = true
dt_min = 0.0005       # fs
dt_max = 0.01         # fs

# Barrier: Gaussian barrier
barrier_type = gaussian
barrier_height = 4.0  # eV
barrier_width = 0.8   # nm
barrier_center = 0.0  # nm

# Particle: Free electron
particle_mass = 1.0   # m_e

# Initial state
x0 = -8.0             # nm 
k0 = 3.5              # nm^-1 (v ~ 3.5 nm/fs, travels ~21 nm in 6 fs)
sigma = 0.6           # nm

# Environment - Idealized (no dissipation)
noise_amplitude = 0.0
noise_correlation_time = 0.4
decoherence_rate = 0.0

# Absorbing boundaries - Scaled with domain
boundary_width = 3.0          # CHANGED from 2.5 (scaled with domain)
boundary_strength = 0.03      # Gentle absorption

# Output
save_netcdf = true
save_animation = true
fps = 30
dpi = 100
