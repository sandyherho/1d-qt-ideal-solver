# Case 1: Field Emission (Rectangular Barrier)
# Standard rectangular potential barrier
# FINAL FIX: Doubled domain size to prevent wavepacket hitting boundaries

scenario_name = Case 1 - Rectangular Barrier

# Spatial grid - CRITICAL FIX: Domain doubled
nx = 2048
x_min = -30.0         # CHANGED from -15.0 (doubled)
x_max = 30.0          # CHANGED from 15.0 (doubled)

# Time integration
t_final = 6.0         # fs
n_snapshots = 200
adaptive_dt = true
dt_min = 0.0005       # fs
dt_max = 0.01         # fs

# Barrier: Rectangular barrier
barrier_type = rectangular
barrier_height = 4.5  # eV
barrier_width = 1.0   # nm
barrier_center = 0.0  # nm

# Particle: Free electron
particle_mass = 1.0   # m_e

# Initial state
x0 = -8.0             # nm (now has 22 nm before left boundary)
k0 = 4.0              # nm^-1 (v ~ 4 nm/fs, travels ~24 nm in 6 fs)
sigma = 0.8           # nm

# Environment - Idealized (no dissipation)
noise_amplitude = 0.0
noise_correlation_time = 0.5
decoherence_rate = 0.0

# Absorbing boundaries - Scaled with domain
boundary_width = 3.0          # CHANGED from 2.5 (scaled with domain)
boundary_strength = 0.03      # Gentle absorption

# Output
save_netcdf = true
save_animation = true
fps = 30
dpi = 100
