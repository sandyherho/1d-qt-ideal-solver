# Case 1: Field Emission (Rectangular Barrier)
# Standard rectangular potential barrier
# REVISED: Reduced noise and decoherence for idealized tunneling

scenario_name = Case 1 - Rectangular Barrier

# Spatial grid
nx = 2048
x_min = -15.0         # nm
x_max = 15.0          # nm

# Time integration
t_final = 6.0         # fs
n_snapshots = 200
adaptive_dt = true
dt_min = 0.0005       # fs
dt_max = 0.01         # fs

# Barrier: Rectangular barrier
barrier_type = rectangular
barrier_height = 4.5  # eV
barrier_width = 1.0   # nm
barrier_center = 0.0  # nm

# Particle: Free electron
particle_mass = 1.0   # m_e

# Initial state
x0 = -8.0             # nm
k0 = 4.0              # nm^-1
sigma = 0.8           # nm

# Environment - REVISED: Nearly ideal (minimal dissipation)
# For truly idealized solver, set all to 0.0
# Small values kept for optional environmental effects
noise_amplitude = 0.0         # Disabled for idealized case
noise_correlation_time = 0.5
decoherence_rate = 0.0        # Disabled for idealized case

# Absorbing boundaries - REVISED: Gentler absorption
boundary_width = 2.5          # Increased from 2.0
boundary_strength = 0.03      # Reduced from 0.1

# Output
save_netcdf = true
save_animation = true
fps = 30
dpi = 100
